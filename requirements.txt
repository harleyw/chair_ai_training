# ============================================
# Ergonomic Chair AI Training - Dependencies
# Version: 1.0.1
# ============================================

# ---------- Core Dependencies (Required) ----------
# Physics simulation engine
pybullet>=3.2.5

# Numerical computing
numpy>=1.21.0

# Deep learning framework (CPU version for training)
torch>=1.10.0

# Reinforcement learning environment interface
gymnasium>=0.28.0

# PPO algorithm implementation
stable-baselines3>=2.0.0

# Data visualization (for evaluation plots)
matplotlib>=3.5.0


# ---------- Optional Dependencies (Enhanced Features) ----------

# TensorBoard - Training visualization and monitoring
# Install: pip install tensorboard
# Usage: Automatically detected by train.py, graceful fallback if missing
tensorboard>=2.10.0

# Progress bar display
# Install: pip install tqdm rich
# Usage: Automatically detected by train.py, graceful fallback if missing
tqdm>=4.62.0
rich>=13.0.0


# ---------- Development/Testing Dependencies ----------

# Data manipulation (for analysis)
pandas>=1.3.0

# Image processing (for sensor visualization)
pillow>=8.0.0


# ---------- Web API Dependencies (Optional) ----------

# FastAPI - High-performance web framework for building APIs
# Install: pip install fastapi uvicorn
# Usage: Run `python -m api.main` or `bash run_api.sh`
fastapi>=0.100.0

# Uvicorn - ASGI server for running FastAPI applications
uvicorn[standard]>=0.23.0

# Pydantic - Data validation using Python type hints (included with FastAPI)
pydantic>=2.0.0


# ---------- ONNX Export Dependencies (Optional) ----------
# ONNX - Open Neural Network Exchange format support
# Install: pip install onnx onnxruntime
onnx>=1.14.0
onnxruntime>=1.15.0
